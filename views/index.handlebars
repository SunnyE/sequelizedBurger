<div>
	<div class='col-md-12'>
		<div id="createBurger">
			<form class="form-inline" method='POST' action='/create'>
				<div class="form-group">
                    <h2>burger Creation station!!!!</h2>
					<label for="burgerName">Name thy Burger</label>
					<input type="text" class="form-control" name='burger_name' id="burgerName" placeholder="Baconator">
				</div>
				<button type="submit" class="btn btn-default"> Send it to the Cook!!!</button>
			</form>
		</div>
	</div>
</div>
<div class='row'>

	<div class='col-md-6'>
		<div class="burgers">
			<h2>Available Burgers</h2>
			{{#each new}}
				<div class='burgerholders'>
					<form method="POST" action="/devoured/{{id}}">
						<p>{{burger_name}}</p>
						<input type="hidden" name="id" value="{{id}}">
						<button type='submit'>Devour</button>
					</form>
				</div>
			{{/each}}
		</div>
	</div>

	<div class='col-md-6'>
		<div class="burgers">
			<h2>Devoured Burgers</h2>
			{{#each devoured}}
				<div class='burgerholders'>
					<p>{{burger_name}}</p>
				</div>
			{{/each}}
		</div>
	</div>

</div>